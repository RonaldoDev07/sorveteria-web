{
  "buildCommand": "if cd flutter 2>/dev/null; then cd ..; else git clone https://github.com/flutter/flutter.git -b stable --depth 1; fi && flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "installCommand": "echo 'Flutter will be installed during build'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
