name: estoque_mobile
description: Sorveteria Camila - Sistema de Controle de Estoque
publish_to: 'none'
version: 1.2.8+21
# FORCE REBUILD: 2026-03-12 - Timezone corrigido (Acre UTC-5) + Histórico detalhado

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  intl: 0.19.0
  image_picker: ^1.0.7
  image_picker_for_web: ^3.0.0
  mobile_scanner: ^5.1.1
  connectivity_plus: ^6.0.0
  audioplayers: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/cash_register.mp3
    - assets/sounds/error.mp3
    - assets/sounds/click.mp3
